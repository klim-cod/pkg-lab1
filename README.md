# Лабораторная работа №1  
## «Цветовые модели: RGB ↔ Lab ↔ CMYK»

[![JavaScript](https://img.shields.io/badge/JavaScript-ES6+-yellow.svg)](https://developer.mozilla.org/ru/docs/Web/JavaScript)
[![HTML5](https://img.shields.io/badge/HTML5-Latest-orange.svg)](https://developer.mozilla.org/ru/docs/Web/HTML)
[![CSS3](https://img.shields.io/badge/CSS3-Latest-blue.svg)](https://developer.mozilla.org/ru/docs/Web/CSS)

Интерактивное веб-приложение для конвертации цветов между моделями RGB, Lab и CMYK с визуализацией в реальном времени.


## 🎯 Цель работы

Изучить принципы работы цветовых моделей RGB, Lab и CMYK, реализовать алгоритмы преобразования между ними, а также разработать интерактивное веб-приложение для визуализации и конвертации цветов в реальном времени.

## 📚 Теоретическая часть

### Цветовые модели

#### 1. Модель RGB (Red, Green, Blue)
- **Тип**: Аддитивная цветовая модель
- **Применение**: Электронные дисплеи, мониторы, телевизоры
- **Принцип**: Цвет формируется сложением основных цветов
- **Диапазон**: 0-255 для каждого канала
- **Формула**: `Цвет = R + G + B`

#### 2. Модель Lab (CIELAB)
- **Тип**: Перцепционно-равномерное цветовое пространство
- **Разработчик**: Международная комиссия по освещению (CIE)
- **Компоненты**:
  - **L***: светлота (0 = черный, 100 = белый)
  - **a***: зелено-красная ось (-128 = зеленый, +127 = красный)
  - **b***: сине-желтая ось (-128 = синий, +127 = желтый)
- **Преимущество**: Независимость от устройства вывода

#### 3. Модель CMYK (Cyan, Magenta, Yellow, Key/Black)
- **Тип**: Субтрактивная цветовая модель
- **Применение**: Полиграфия, печать
- **Принцип**: Цвет формируется вычитанием из белого света
- **Диапазон**: 0-100% для каждого компонента
- **Формула**: `Цвет = Белый - C - M - Y`

## 🔄 Алгоритмы преобразований

### Ключевые преобразования

```javascript
// RGB → CMYK
function rgbToCmyk(r, g, b) {
    const k = 1 - Math.max(r/255, g/255, b/255);
    if (k === 1) return { c: 0, m: 0, y: 0, k: 100 };
    return {
        c: Math.round((1 - r/255 - k) / (1 - k) * 100),
        m: Math.round((1 - g/255 - k) / (1 - k) * 100),
        y: Math.round((1 - b/255 - k) / (1 - k) * 100),
        k: Math.round(k * 100)
    };
}

// CMYK → RGB
function cmykToRgb(c, m, y, k) {
    return {
        r: Math.round(255 * (1 - c/100) * (1 - k/100)),
        g: Math.round(255 * (1 - m/100) * (1 - k/100)),
        b: Math.round(255 * (1 - y/100) * (1 - k/100))
    };
}
// Точка белого D65
const whitePoint = { x: 95.047, y: 100, z: 108.883 };

// RGB → XYZ → Lab
function rgbToLab(r, g, b) {
    const xyz = rgbToXyz(r, g, b);
    return xyzToLab(xyz.x, xyz.y, xyz.z);
}

// Lab → XYZ → RGB
function labToRgb(l, a, b) {
    const xyz = labToXyz(l, a, b);
    return xyzToRgb(xyz.x, xyz.y, xyz.z);
}
```

### Логика преобразований  
- **RGB ↔ CMYK**: Используются формулы, основанные на нормализованных значениях  
- **RGB ↔ Lab**: Преобразование через промежуточную модель XYZ с использованием стандартных коэффициентов  
- **Обработка граничных значений**: При выходе за пределы допустимых диапазонов выводятся предупреждения  

### Примеры преобразований  

| Исходный цвет (RGB) | Lab | CMYK |
|---------------------|-----|------|
| `#FF0000` (255,0,0) | `(53.24, 80.09, 67.20)` | `(0%, 100%, 100%, 0%)` |
| `#00FF00` (0,255,0) | `(87.73, -86.18, 83.18)` | `(100%, 0%, 100%, 0%)` |
| `#0000FF` (0,0,255) | `(32.30, 79.19, -107.86)` | `(100%, 100%, 0%, 0%)` |

---

## Особенности реализации  
- Все преобразования выполняются на стороне клиента  
- Поддержка ввода через ползунки, числовые поля и палитру  
- Автоматическая валидация и коррекция введенных значений  

---


## Выводы  
1. Разработанное веб-приложение позволяет интерактивно исследовать цветовые модели RGB, Lab и CMYK  
2. Реализованы точные алгоритмы конвертации между моделями с учетом граничных условий  
3. Интерфейс обеспечивает удобное взаимодействие: выбор цвета, точный ввод, плавное изменение через ползунки  
